<h1>All The Guests</h1>
<h4><%= link_to "Create a Guest Appearance", new_appearance_path %></h4>
<ul>
  <% @guests.each do |guest| %>
    <li><%= link_to guest.name, guest_path(guest) %></li>
  <% end %>
</ul>
